# adapt-authoring-courseassets

Tracks which [assets](../adapt-authoring-assets) are used by which courses. Maintains the course-to-asset relationship automatically by responding to content events, so asset usage stays accurate as content changes.

Extends `AbstractApiModule` from [adapt-authoring-core](../adapt-authoring-core).
